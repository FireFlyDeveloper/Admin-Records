{
  "name": "dragonfly-platform",
  "version": "1.0.0",
  "description": "Document + Hybrid Inventory Platform",
  "main": "dist/index.js",
  "scripts": {
    "dev": "nodemon src/index.ts",
    "build": "tsc -p tsconfig.build.json && npm run build:scripts",
    "build:scripts": "mkdir -p temp_scripts && tsc scripts/migrate.ts scripts/seed.ts scripts/migrationHelpers.ts --outDir . --target ES2022 --module commonjs --esModuleInterop --skipLibCheck --ignoreConfig && find . -maxdepth 1 -name '*.js' -exec mv {} dist/ \\; 2>/dev/null && rm -rf temp_scripts",
    "start": "node dist/index.js",
    "migrate": "npm run build && node dist/scripts/migrate.js",
    "migrate:rollback": "npm run build && node dist/scripts/migrate.js rollback",
    "migrate:status": "npm run build && node dist/scripts/migrate.js status",
    "migrate:create": "npm run build && node dist/scripts/migrate.js create",
    "seed": "npm run build && node dist/scripts/seed.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --ci --coverage --maxWorkers=2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/morgan": "^1.9.10",
    "@types/multer": "^2.1.0",
    "@types/node": "^25.6.0",
    "@types/nodemailer": "^8.0.0",
    "@types/pg": "^8.20.0",
    "@types/uuid": "^10.0.0",
    "@types/ws": "^8.18.1",
    "nodemon": "^3.1.14",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "exceljs": "^4.4.0",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.10.1",
    "mqtt": "^5.15.1",
    "multer": "^2.1.1",
    "nodemailer": "^8.0.7",
    "pg": "^8.20.0",
    "uuid": "^14.0.0",
    "ws": "^8.20.0"
  }
}
