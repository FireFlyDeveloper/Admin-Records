# Docker internal network — trust admin for migrations
# (more restrictive than the alpine image's default scram-sha-256 line)
host    all             admin           0.0.0.0/0               trust
host    all             admin           ::/0                    trust
