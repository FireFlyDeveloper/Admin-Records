# Frontend configuration — local Docker stack
VITE_APP_NAME=Dragonfly Platform
VITE_MAX_UPLOAD_SIZE=104857600
VITE_DEFAULT_PAGE_SIZE=25
# API goes through the frontend's own nginx, same-origin, on /api prefix.
VITE_API_BASE_URL=/api
VITE_WS_URL=ws://localhost:8180/ws
